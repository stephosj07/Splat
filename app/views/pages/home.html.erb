<div class="banner">
    <h1>Splat</h1>

      <h2><span id="banner-typed-text"></span></h2>

      <span id="banner-typed-text"></span>
      <div class="search">
        <%= form_tag camping_spots_path, method: :get do %>
            <%= text_field_tag :query,
            params[:query],
            class: "form-control",
            placeholder: "Find a camping spot"
            %>
          <%= submit_tag "Search", class: "btn btn-primary" %>
        <% end %>
      </div>
</div>
